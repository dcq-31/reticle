/** Maximum device-pixel-ratio to render at (avoids GPU overload on 3x screens). */
export const MAX_DPR = 2;

/** Minimum zoom factor for 2D viewports. */
export const ZOOM_MIN = 0.2;

/** Maximum zoom factor for 2D viewports. */
export const ZOOM_MAX = 12;

/** CSS media query for the desktop breakpoint (>= 1024 px). */
export const DESKTOP_BP = "(min-width: 1024px)";
